import * as path from 'path';

export const collectionDir = path.join(__dirname, '..', 'collection');
export const traitsDir = path.join(collectionDir, 'traits');
export const outputDir = path.join(__dirname, '..', '..', 'output');
export const outputImageDir = path.join(outputDir, 'images');
export const outputMetadataDir = path.join(outputDir, 'metadata');
