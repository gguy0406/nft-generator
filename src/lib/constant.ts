import * as path from 'path';

export const collectionDir = path.join(__dirname, '..', 'collection');
export const outputImageDir = path.join(collectionDir, 'output', 'images');
export const outputMetadataDir = path.join(collectionDir, 'output', 'metadata');
